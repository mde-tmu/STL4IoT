/** Generated by itemis CREATE code generator. */
/* Implements a standard statechart trace handler which adapts to the
 * YET tracing library.
 */


#include "Hub_PowerManager_tracer.h"
#include "Hub_PowerManager_meta.h"

#include <string.h>
#include <stdlib.h>


#ifndef SC_UNUSED
#define SC_UNUSED(P) (void)(P)
#endif

static sc_string default_scope_name = "Hub_PowerManager";

static yet_value_serializer feature_value_serializer[10];



static void init_feature_value_serializer() {
	feature_value_serializer[0] = yet_serialize_void;
	feature_value_serializer[1] = yet_serialize_void;
	feature_value_serializer[2] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[3] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[4] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[5] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[6] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[7] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[8] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[9] = yet_serialize_bool;
}

static char* featureName(sc_integer featureId)
{
	return Hub_PowerManager_meta_feature_names[featureId];
}

static char* featureValue(sc_integer featureId, const void* valuePtr, char* buf)
{
	return (feature_value_serializer[featureId])(valuePtr, buf);
}

static char* stateName(sc_integer stateId)
{
	return Hub_PowerManager_meta_state_names[stateId];
}

static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn);

void hub_PowerManager_init_sc_tracer(yet_sc_tracer *tracer, Hub_PowerManager* machine, sc_string name)
{
	if (tracer != sc_null) {
		yet_init_sc_tracer(tracer, machine, &dispatchMessage);

		tracer->scope.name = default_scope_name;
		if (name != sc_null) {
			tracer->scope.name = name;
		}
		tracer->traceinfoProvider.featureName = featureName;
		tracer->traceinfoProvider.featureValue = featureValue;
		tracer->traceinfoProvider.stateName = stateName;
		
		machine->trace_handler = &(tracer->traceEventHandler);
	}
	init_feature_value_serializer();
}


/* Implementation of yet_handler callback function. Handles incoming stimuli and call appropriate state machine function. */
static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn)
{
	yet_sc_tracer* tracer = scope->instance;
	char* member;
	member = fqn;
	
	
	if(strcmp(member, "threshold_reached") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		hub_PowerManager_raise_threshold_reached(tracer->machine);
		return 0;
	}
	if(strcmp(member, "total") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_total(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "threshold") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_threshold(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "prev_last_value") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_prev_last_value(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "last_value") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_last_value(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "previous_device_index") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_previous_device_index(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "device_index") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_device_index(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "system_index_to_turnoff") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		hub_PowerManager_set_system_index_to_turnoff(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "over_threshold") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		hub_PowerManager_set_over_threshold(tracer->machine, converted);
		return 0;
	}
	
	
	return YET_ERR_INVALID_KEY;
}

