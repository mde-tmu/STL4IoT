/** Generated by itemis CREATE code generator. */
/* Implements a standard statechart trace handler which adapts to the
 * YET tracing library.
 */


#include "Elevator_Unit_tracer.h"
#include "Elevator_Unit_meta.h"

#include <string.h>
#include <stdlib.h>


#ifndef SC_UNUSED
#define SC_UNUSED(P) (void)(P)
#endif

static sc_string default_scope_name = "Elevator_Unit";

static yet_value_serializer feature_value_serializer[13];



static void init_feature_value_serializer() {
	feature_value_serializer[0] = yet_serialize_void;
	feature_value_serializer[1] = yet_serialize_void;
	feature_value_serializer[2] = yet_serialize_void;
	feature_value_serializer[3] = yet_serialize_void;
	feature_value_serializer[4] = yet_serialize_void;
	feature_value_serializer[5] = yet_serialize_void;
	feature_value_serializer[6] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[7] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[8] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[9] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[10] = yet_serialize_void;
	feature_value_serializer[11] = yet_serialize_void;
	feature_value_serializer[12] = yet_serialize_void;
}

static char* featureName(sc_integer featureId)
{
	return Elevator_Unit_meta_feature_names[featureId];
}

static char* featureValue(sc_integer featureId, const void* valuePtr, char* buf)
{
	return (feature_value_serializer[featureId])(valuePtr, buf);
}

static char* stateName(sc_integer stateId)
{
	return Elevator_Unit_meta_state_names[stateId];
}

static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn);

void elevator_Unit_init_sc_tracer(yet_sc_tracer *tracer, Elevator_Unit* machine, sc_string name)
{
	if (tracer != sc_null) {
		yet_init_sc_tracer(tracer, machine, &dispatchMessage);

		tracer->scope.name = default_scope_name;
		if (name != sc_null) {
			tracer->scope.name = name;
		}
		tracer->traceinfoProvider.featureName = featureName;
		tracer->traceinfoProvider.featureValue = featureValue;
		tracer->traceinfoProvider.stateName = stateName;
		
		machine->trace_handler = &(tracer->traceEventHandler);
	}
	init_feature_value_serializer();
}


/* Implementation of yet_handler callback function. Handles incoming stimuli and call appropriate state machine function. */
static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn)
{
	yet_sc_tracer* tracer = scope->instance;
	char* member;
	member = fqn;
	
	
	if(strcmp(member, "toggle") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_raise_toggle(tracer->machine);
		return 0;
	}
	if(strcmp(member, "stopped") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_raise_stopped(tracer->machine);
		return 0;
	}
	if(strcmp(member, "inUse") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_raise_inUse(tracer->machine);
		return 0;
	}
	if(strcmp(member, "direction.up") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_direction_raise_up(tracer->machine);
		return 0;
	}
	if(strcmp(member, "direction.down") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_direction_raise_down(tracer->machine);
		return 0;
	}
	if(strcmp(member, "input.submit") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_input_raise_submit(tracer->machine);
		return 0;
	}
	if(strcmp(member, "input.up") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_input_raise_up(tracer->machine);
		return 0;
	}
	if(strcmp(member, "input.down") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		elevator_Unit_input_raise_down(tracer->machine);
		return 0;
	}
	if(strcmp(member, "level.current") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		elevator_Unit_level_set_current(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "level.userLocation") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		elevator_Unit_level_set_userLocation(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "level.userInput") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		elevator_Unit_level_set_userInput(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "input.value") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		elevator_Unit_input_set_value(tracer->machine, converted);
		return 0;
	}
	
	
	return YET_ERR_INVALID_KEY;
}

