/** Generated by itemis CREATE code generator. */
/* Implements a standard statechart trace handler which adapts to the
 * YET tracing library.
 */


#include "SmartFireSystem1_tracer.h"
#include "SmartFireSystem1_meta.h"

#include <string.h>
#include <stdlib.h>


#ifndef SC_UNUSED
#define SC_UNUSED(P) (void)(P)
#endif

static sc_string default_scope_name = "SmartFireSystem1";

static yet_value_serializer feature_value_serializer[40];



static void init_feature_value_serializer() {
	feature_value_serializer[0] = yet_serialize_void;
	feature_value_serializer[1] = yet_serialize_void;
	feature_value_serializer[2] = yet_serialize_void;
	feature_value_serializer[3] = yet_serialize_void;
	feature_value_serializer[4] = yet_serialize_void;
	feature_value_serializer[5] = yet_serialize_void;
	feature_value_serializer[6] = yet_serialize_void;
	feature_value_serializer[7] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[8] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[9] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[10] = yet_serialize_string;
	feature_value_serializer[11] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[12] = yet_serialize_string;
	feature_value_serializer[13] = yet_serialize_string;
	feature_value_serializer[14] = yet_serialize_void;
	feature_value_serializer[15] = yet_serialize_void;
	feature_value_serializer[16] = yet_serialize_void;
	feature_value_serializer[17] = yet_serialize_void;
	feature_value_serializer[18] = yet_serialize_void;
	feature_value_serializer[19] = yet_serialize_void;
	feature_value_serializer[20] = yet_serialize_bool;
	feature_value_serializer[21] = yet_serialize_bool;
	feature_value_serializer[22] = yet_serialize_void;
	feature_value_serializer[23] = yet_serialize_void;
	feature_value_serializer[24] = yet_serialize_void;
	feature_value_serializer[25] = yet_serialize_bool;
	feature_value_serializer[26] = yet_serialize_bool;
	feature_value_serializer[27] = yet_serialize_bool;
	feature_value_serializer[28] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[29] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[30] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[31] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[32] = yet_serialize_void;
	feature_value_serializer[33] = yet_serialize_void;
	feature_value_serializer[34] = yet_serialize_void;
	feature_value_serializer[35] = yet_serialize_bool;
	feature_value_serializer[36] = yet_serialize_bool;
	feature_value_serializer[37] = yet_serialize_void;
	feature_value_serializer[38] = yet_serialize_void;
	feature_value_serializer[39] = yet_serialize_void;
}

static char* featureName(sc_integer featureId)
{
	return SmartFireSystem1_meta_feature_names[featureId];
}

static char* featureValue(sc_integer featureId, const void* valuePtr, char* buf)
{
	return (feature_value_serializer[featureId])(valuePtr, buf);
}

static char* stateName(sc_integer stateId)
{
	return SmartFireSystem1_meta_state_names[stateId];
}

static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn);

void smartFireSystem1_init_sc_tracer(yet_sc_tracer *tracer, SmartFireSystem1* machine, sc_string name)
{
	if (tracer != sc_null) {
		yet_init_sc_tracer(tracer, machine, &dispatchMessage);

		tracer->scope.name = default_scope_name;
		if (name != sc_null) {
			tracer->scope.name = name;
		}
		tracer->traceinfoProvider.featureName = featureName;
		tracer->traceinfoProvider.featureValue = featureValue;
		tracer->traceinfoProvider.stateName = stateName;
		
		machine->trace_handler = &(tracer->traceEventHandler);
	}
	init_feature_value_serializer();
}


/* Implementation of yet_handler callback function. Handles incoming stimuli and call appropriate state machine function. */
static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn)
{
	yet_sc_tracer* tracer = scope->instance;
	char* member;
	member = fqn;
	
	
	if(strcmp(member, "toggle") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_raise_toggle(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Timer.enable") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Timer_raise_enable(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Timer.disable") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Timer_raise_disable(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Timer.reset") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Timer_raise_reset(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Mode.manual_alarm_off") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Mode_raise_manual_alarm_off(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Mode.manual_alarm_on") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Mode_raise_manual_alarm_on(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Detect.smoke") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Detect_raise_smoke(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Detect.carbon") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Detect_raise_carbon(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Detect.both") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		smartFireSystem1_Detect_raise_both(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Timer.counter") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Timer_set_counter(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Timer.warning_period") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Timer_set_warning_period(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Timer.final_warning_period") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Timer_set_final_warning_period(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Power.kilowatt") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Power_set_kilowatt(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Message.status") == 0) {
		sc_string converted = msg->value;
		smartFireSystem1_Message_set_status(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Message.sensor") == 0) {
		sc_string converted = msg->value;
		smartFireSystem1_Message_set_sensor(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Alarm.threshold_reached") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Alarm_set_threshold_reached(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Alarm.sound") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Alarm_set_sound(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.smoke_standby") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Sensors_set_smoke_standby(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.carbon_standby") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Sensors_set_carbon_standby(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.fire_standby") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Sensors_set_fire_standby(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.smoke_value") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Sensors_set_smoke_value(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.carbon_value") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Sensors_set_carbon_value(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.smoke_threshold") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Sensors_set_smoke_threshold(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Sensors.carbon_threshold") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		smartFireSystem1_Sensors_set_carbon_threshold(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Detect.smokeDetected") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Detect_set_smokeDetected(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Detect.carbonDetected") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		smartFireSystem1_Detect_set_carbonDetected(tracer->machine, converted);
		return 0;
	}
	
	
	return YET_ERR_INVALID_KEY;
}

