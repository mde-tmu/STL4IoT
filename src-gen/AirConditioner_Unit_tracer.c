/** Generated by itemis CREATE code generator. */
/* Implements a standard statechart trace handler which adapts to the
 * YET tracing library.
 */


#include "AirConditioner_Unit_tracer.h"
#include "AirConditioner_Unit_meta.h"

#include <string.h>
#include <stdlib.h>


#ifndef SC_UNUSED
#define SC_UNUSED(P) (void)(P)
#endif

static sc_string default_scope_name = "AirConditioner_Unit";

static yet_value_serializer feature_value_serializer[15];



static void init_feature_value_serializer() {
	feature_value_serializer[0] = yet_serialize_void;
	feature_value_serializer[1] = yet_serialize_void;
	feature_value_serializer[2] = yet_serialize_void;
	feature_value_serializer[3] = yet_serialize_void;
	feature_value_serializer[4] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[5] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[6] = yet_serialize_void;
	feature_value_serializer[7] = yet_serialize_void;
	feature_value_serializer[8] = yet_serialize_string;
	feature_value_serializer[9] = yet_serialize_void;
	feature_value_serializer[10] = yet_serialize_void;
	feature_value_serializer[11] = yet_serialize_bool;
	feature_value_serializer[12] = yet_serialize_void;
	feature_value_serializer[13] = yet_serialize_void;
	feature_value_serializer[14] = yet_serialize_bool;
}

static char* featureName(sc_integer featureId)
{
	return AirConditioner_Unit_meta_feature_names[featureId];
}

static char* featureValue(sc_integer featureId, const void* valuePtr, char* buf)
{
	return (feature_value_serializer[featureId])(valuePtr, buf);
}

static char* stateName(sc_integer stateId)
{
	return AirConditioner_Unit_meta_state_names[stateId];
}

static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn);

void airConditioner_Unit_init_sc_tracer(yet_sc_tracer *tracer, AirConditioner_Unit* machine, sc_string name)
{
	if (tracer != sc_null) {
		yet_init_sc_tracer(tracer, machine, &dispatchMessage);

		tracer->scope.name = default_scope_name;
		if (name != sc_null) {
			tracer->scope.name = name;
		}
		tracer->traceinfoProvider.featureName = featureName;
		tracer->traceinfoProvider.featureValue = featureValue;
		tracer->traceinfoProvider.stateName = stateName;
		
		machine->trace_handler = &(tracer->traceEventHandler);
	}
	init_feature_value_serializer();
}


/* Implementation of yet_handler callback function. Handles incoming stimuli and call appropriate state machine function. */
static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn)
{
	yet_sc_tracer* tracer = scope->instance;
	char* member;
	member = fqn;
	
	
	if(strcmp(member, "toggle") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_raise_toggle(tracer->machine);
		return 0;
	}
	if(strcmp(member, "temp_up") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_raise_temp_up(tracer->machine);
		return 0;
	}
	if(strcmp(member, "temp_down") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_raise_temp_down(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Metric.convertToFahrenheit") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_Metric_raise_convertToFahrenheit(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Metric.convertToCelsius") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_Metric_raise_convertToCelsius(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Heat.increase") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_Heat_raise_increase(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Heat.decrease") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_Heat_raise_decrease(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Cool.increase") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_Cool_raise_increase(tracer->machine);
		return 0;
	}
	if(strcmp(member, "Cool.decrease") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		airConditioner_Unit_Cool_raise_decrease(tracer->machine);
		return 0;
	}
	if(strcmp(member, "tempChange") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		airConditioner_Unit_set_tempChange(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "RoomTemp") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		airConditioner_Unit_set_roomTemp(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Metric.temp") == 0) {
		sc_string converted = msg->value;
		airConditioner_Unit_Metric_set_temp(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Heat.on") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		airConditioner_Unit_Heat_set_on(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Cool.on") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		airConditioner_Unit_Cool_set_on(tracer->machine, converted);
		return 0;
	}
	
	
	return YET_ERR_INVALID_KEY;
}

