/** Generated by itemis CREATE code generator. */
/* Implements a standard statechart trace handler which adapts to the
 * YET tracing library.
 */


#include "DeviceTemp_Component_tracer.h"
#include "DeviceTemp_Component_meta.h"

#include <string.h>
#include <stdlib.h>


#ifndef SC_UNUSED
#define SC_UNUSED(P) (void)(P)
#endif

static sc_string default_scope_name = "DeviceTemp_Component";

static yet_value_serializer feature_value_serializer[12];



static void init_feature_value_serializer() {
	feature_value_serializer[0] = yet_serialize_void;
	feature_value_serializer[1] = yet_serialize_string;
	feature_value_serializer[2] = yet_serialize_bool;
	feature_value_serializer[3] = yet_serialize_bool;
	feature_value_serializer[4] = yet_serialize_bool;
	feature_value_serializer[5] = yet_serialize_bool;
	feature_value_serializer[6] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[7] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[8] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[9] = yet_int_serializer_function(sizeof(sc_integer), true);
	feature_value_serializer[10] = yet_serialize_bool;
	feature_value_serializer[11] = yet_serialize_void;
}

static char* featureName(sc_integer featureId)
{
	return DeviceTemp_Component_meta_feature_names[featureId];
}

static char* featureValue(sc_integer featureId, const void* valuePtr, char* buf)
{
	return (feature_value_serializer[featureId])(valuePtr, buf);
}

static char* stateName(sc_integer stateId)
{
	return DeviceTemp_Component_meta_state_names[stateId];
}

static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn);

void deviceTemp_Component_init_sc_tracer(yet_sc_tracer *tracer, DeviceTemp_Component* machine, sc_string name)
{
	if (tracer != sc_null) {
		yet_init_sc_tracer(tracer, machine, &dispatchMessage);

		tracer->scope.name = default_scope_name;
		if (name != sc_null) {
			tracer->scope.name = name;
		}
		tracer->traceinfoProvider.featureName = featureName;
		tracer->traceinfoProvider.featureValue = featureValue;
		tracer->traceinfoProvider.stateName = stateName;
		
		machine->trace_handler = &(tracer->traceEventHandler);
	}
	init_feature_value_serializer();
}


/* Implementation of yet_handler callback function. Handles incoming stimuli and call appropriate state machine function. */
static yet_error dispatchMessage(yet_scope *scope, yet_message * msg, char *fqn)
{
	yet_sc_tracer* tracer = scope->instance;
	char* member;
	member = fqn;
	
	
	if(strcmp(member, "Heat.continue") == 0) {
		/* Skip one message; this should not be re-raised on host */
		/* TODO: this is not optimal check if we can get rid of it */
		tracer->skip_raised_in_event++; 
		deviceTemp_Component_Heat_raise_continue(tracer->machine);
		return 0;
	}
	if(strcmp(member, "message") == 0) {
		sc_string converted = msg->value;
		deviceTemp_Component_set_message(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "fully_rested") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		deviceTemp_Component_set_fully_rested(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "device.isOn") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		deviceTemp_Component_device_set_isOn(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "device.cooling_down") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		deviceTemp_Component_device_set_cooling_down(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "device.heating_up") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		deviceTemp_Component_device_set_heating_up(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Heat.level") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		deviceTemp_Component_Heat_set_level(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Heat.increment") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		deviceTemp_Component_Heat_set_increment(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Heat.decrement") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		deviceTemp_Component_Heat_set_decrement(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Heat.max") == 0) {
		sc_integer converted = (sc_integer)atol(msg->value);
		deviceTemp_Component_Heat_set_max(tracer->machine, converted);
		return 0;
	}
	if(strcmp(member, "Heat.user_warned") == 0) {
		sc_boolean converted = (msg->value[0] == 't');
		deviceTemp_Component_Heat_set_user_warned(tracer->machine, converted);
		return 0;
	}
	
	
	return YET_ERR_INVALID_KEY;
}

